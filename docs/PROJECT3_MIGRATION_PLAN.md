# 專案1 整合遷移計劃

## 📋 概述

**直接在專案1上整合專案2的sys管理功能**，保留專案1的會員與權限系統，然後逐步補齊功能。

> 💡 **為什麼直接在專案1上修改？**
> - 專案1已有基礎架構和RBAC系統
> - 功能不多，歷史包袱不重
> - 避免重複建立專案，節省時間
> - 可以直接在現有基礎上擴展

## 🎯 遷移目標

1. **專案2的sys相關功能** → **專案1**
   - sys_admin（管理員表）- 整合到現有的 users 表或新增
   - sys_module（模組表）- 整合到現有的模組系統
   - sys_structure（結構表）- 整合到現有的結構系統
   - sys_roles（角色表）- 專案1已有，檢查是否需要補充欄位
   - sys_permissions（權限表）- 專案1已有，整合權限資料
   - sys_role_permissions（角色權限關聯）- 專案1已有
   - sys_user_roles（使用者角色關聯）- 專案1已有
   - sys_user_permissions（使用者權限關聯）- 專案1已有

2. **保留專案1的會員與權限系統**
   - 會員相關權限（member.view, member.create, member.edit, member.delete等）
   - 區域權限（tw, sg, mm）
   - 現有的 RBAC 系統架構

## 📁 專案結構（專案1整合後）

```
myanmar-nuxt4/  # 直接在專案1上修改
├── admin/              # Nuxt 4 前端
│   ├── app/
│   │   ├── pages/
│   │   │   ├── system/        # 從專案2遷移
│   │   │   │   ├── admins.vue
│   │   │   │   ├── module.vue
│   │   │   │   ├── structure.vue
│   │   │   │   ├── roles.vue
│   │   │   │   └── permissions.vue
│   │   │   └── member/         # 從專案1遷移（待實作）
│   │   ├── composables/
│   │   │   ├── useAuth.ts      # 整合兩個專案的認證
│   │   │   ├── usePermission.ts # 整合權限檢查
│   │   │   ├── useRole.ts      # 從專案2遷移
│   │   │   └── useMember.ts    # 從專案1遷移（待實作）
│   │   └── components/
│   │       ├── Permission/     # 從專案2遷移
│   │       └── Role/           # 從專案2遷移
│   └── nuxt.config.ts
├── api/                # CodeIgniter 4 後端
│   ├── app/
│   │   ├── Controllers/
│   │   │   ├── AuthController.php      # 整合認證
│   │   │   ├── AdminsController.php    # 從專案2遷移
│   │   │   ├── ModuleController.php    # 從專案2遷移
│   │   │   ├── StructureController.php # 從專案2遷移
│   │   │   ├── RoleController.php      # 從專案2遷移
│   │   │   ├── PermissionController.php # 從專案2遷移
│   │   │   └── MemberController.php    # 從專案1遷移（待實作）
│   │   └── Models/
│   │       ├── SysAdminModel.php       # 從專案2遷移
│   │       ├── SysModuleModel.php      # 整合兩個專案
│   │       ├── SysStructureModel.php   # 整合兩個專案
│   │       ├── RoleModel.php           # 從專案2遷移
│   │       ├── PermissionModel.php     # 從專案2遷移
│   │       └── MemberModel.php         # 從專案1遷移（待實作）
│   └── composer.json
└── DB/
    ├── init/              # 初始化SQL
    │   ├── 01-sys-tables.sql      # 專案2的sys表
    │   ├── 02-rbac-tables.sql     # RBAC系統表
    │   ├── 03-permissions.sql     # 權限資料（整合兩個專案）
    │   └── 04-migration.sql        # 資料遷移腳本
    └── schema.sql         # 完整資料庫架構
```

## 🔄 遷移步驟

### 階段一：準備工作（0.5天）

1. **備份專案1**
   ```bash
   # 建議先備份整個專案
   cp -r myanmar-nuxt4 myanmar-nuxt4-backup
   ```

2. **檢查現有結構**
   - 檢查資料庫表結構
   - 檢查現有的 Controllers 和 Models
   - 檢查前端的 Composables 和 Components

3. **建立遷移分支**（如果使用 Git）
   ```bash
   git checkout -b feature/integrate-project2-sys
   ```

### 階段二：遷移專案2的sys功能（3-5天）

#### 2.1 資料庫遷移

1. **建立sys相關資料表**
   - 從專案2複製 `sys_admin.sql`
   - 從專案2複製 `sys_module.sql`
   - 從專案2複製 `sys_structure.sql`
   - 從專案2複製 `sys_roles.sql`
   - 從專案2複製 `sys_permissions.sql`
   - 從專案2複製 `sys_role_permissions.sql`
   - 從專案2複製 `sys_user_roles.sql`
   - 從專案2複製 `sys_user_permissions.sql`

2. **整合RBAC系統**
   - 專案2的RBAC系統更完整，直接使用
   - 保留專案1的權限命名規範（member.view等）

#### 2.2 後端API遷移

1. **複製Controllers**
   - `AdminsController.php`
   - `ModuleController.php`
   - `StructureController.php`
   - `RoleController.php`
   - `PermissionController.php`

2. **複製Models**
   - `SysAdminModel.php`
   - `RoleModel.php`
   - `PermissionModel.php`
   - `RolePermissionModel.php`
   - `UserRoleModel.php`
   - `UserPermissionModel.php`

3. **整合現有Models**
   - 更新 `SysModuleModel.php`（整合兩個專案的欄位）
   - 更新 `SysStructureModel.php`（整合兩個專案的欄位）

#### 2.3 前端遷移

1. **複製頁面**
   - `admin/app/pages/system/admins/`
   - `admin/app/pages/system/roles.vue`
   - `admin/app/pages/system/permissions.vue`

2. **複製Composables**
   - `useRole.ts`
   - `usePermissionData.ts`
   - 更新 `usePermission.ts`（整合兩個專案的權限檢查邏輯）

3. **複製Components**
   - `Permission/` 目錄
   - `Role/` 目錄
   - `PermissionGuard.vue`

### 階段三：遷移專案1的會員與權限（3-5天）

#### 3.1 權限資料遷移

1. **分析專案1的權限結構**
   - 會員權限：member.view, member.create, member.edit, member.delete
   - 區域權限：member.tw.manage, member.sg.manage, member.mm.manage
   - 租賃會員權限：member.rent.view, member.rent.sg.manage等

2. **建立權限遷移SQL**
   ```sql
   -- 將會員相關權限插入到 sys_permissions 表
   INSERT INTO sys_permissions (name, label, module_id, category, action, status)
   VALUES
   ('member.view', '查看會員', NULL, NULL, 'view', 1),
   ('member.create', '新增會員', NULL, NULL, 'create', 1),
   ('member.edit', '編輯會員', NULL, NULL, 'edit', 1),
   ('member.delete', '刪除會員', NULL, NULL, 'delete', 1),
   ('member.tw.manage', '管理台灣會員', NULL, 'tw', 'manage', 1),
   ('member.sg.manage', '管理新加坡會員', NULL, 'sg', 'manage', 1),
   ('member.mm.manage', '管理緬甸會員', NULL, 'mm', 'manage', 1);
   ```

#### 3.2 使用者資料遷移

1. **從 sys_loginsystem 遷移到 sys_admin**
   ```sql
   INSERT INTO sys_admin (username, password_hash, name, status, created_at, updated_at)
   SELECT 
     loginID,
     password,  -- 需要重新雜湊
     name,
     CASE WHEN expiration_chk = 1 AND expiration_date < CURDATE() THEN 0 ELSE 1 END,
     create_time,
     update_time
   FROM sys_loginsystem;
   ```

2. **權限映射**
   - 分析舊的 power_* 欄位
   - 映射到新的 RBAC 權限系統
   - 建立對應的角色和權限分配

#### 3.3 前端功能遷移（待實作）

1. **會員管理頁面**（待開發）
   - `admin/app/pages/member/index.vue`
   - `admin/app/pages/member/add.vue`
   - `admin/app/pages/member/edit/[id].vue`

2. **Composables**
   - `useMember.ts`（待開發）

### 階段四：整合與測試（2-3天）

1. **統一認證系統**
   - 整合兩個專案的登入邏輯
   - 統一JWT token格式
   - 統一權限檢查機制

2. **測試功能**
   - 測試sys管理功能
   - 測試權限系統
   - 測試使用者登入

3. **文檔更新**
   - 更新API文檔
   - 更新使用手冊

## 📊 資料表對應關係

### 專案1 現有結構

| 專案1現有 | 說明 |
|---------|------|
| permissions | 權限表（已有） |
| roles | 角色表（已有） |
| user_roles | 使用者角色關聯（已有） |
| role_permissions | 角色權限關聯（已有） |
| user_permissions | 使用者權限關聯（已有） |
| sys_loginsystem | 舊的使用者表（保留作為參考） |

### 專案2 → 專案1 整合

| 專案2 | 專案1整合後 | 說明 |
|------|-----------|------|
| sys_admin | users 或 sys_admin | 需要決定：使用現有 users 表或新增 sys_admin |
| sys_module | 整合到現有模組系統 | 檢查欄位差異，統一結構 |
| sys_structure | 整合到現有結構系統 | 檢查欄位差異，統一結構 |
| sys_roles | roles | 專案1已有，檢查是否需要補充欄位 |
| sys_permissions | permissions | 專案1已有，整合權限資料 |
| sys_role_permissions | role_permissions | 專案1已有，直接使用 |
| sys_user_roles | user_roles | 專案1已有，直接使用 |
| sys_user_permissions | user_permissions | 專案1已有，直接使用 |

## ⚠️ 注意事項

1. **資料庫命名**
   - 專案2使用 `sys_` 前綴
   - 專案1使用無前綴（permissions, roles等）
   - **決定**：保持專案1的命名（無前綴），或統一改為 `sys_` 前綴
   - **建議**：如果專案1已有資料，保持現有命名；如果資料不多，可以統一改為 `sys_` 前綴

2. **權限命名規範**
   - 保留專案1的命名規範：`module.category.action`
   - 例如：`member.view`, `member.tw.manage`

3. **密碼處理**
   - 專案1的密碼可能是明文或舊的雜湊方式
   - 需要重新雜湊為 bcrypt

4. **舊系統相容**
   - 保留 `sys_loginsystem` 表作為參考
   - 建立遷移腳本將舊權限映射到新系統

## 🚀 後續開發

完成遷移後，專案1將具備：

1. ✅ 完整的sys管理功能（從專案2整合）
2. ✅ 完整的RBAC權限系統（專案1已有，整合專案2的改進）
3. ✅ 會員相關權限定義（專案1已有）
4. ⏳ 會員管理功能（待開發）
5. ⏳ 其他業務模組（待開發）

## 📝 檢查清單

### 階段一：準備工作
- [ ] 備份專案1
- [ ] 檢查現有結構
- [ ] 建立遷移分支（如使用Git）

### 階段二：sys功能遷移
- [ ] 遷移資料庫表結構
- [ ] 遷移後端Controllers
- [ ] 遷移後端Models
- [ ] 遷移前端頁面
- [ ] 遷移前端Composables
- [ ] 遷移前端Components
- [ ] 測試sys管理功能

### 階段三：會員與權限遷移
- [ ] 遷移權限資料
- [ ] 遷移使用者資料
- [ ] 建立權限映射
- [ ] 測試權限系統

### 階段四：整合與測試
- [ ] 統一認證系統
- [ ] 整合權限檢查
- [ ] 完整功能測試
- [ ] 更新文檔

---

## 💡 為什麼直接在專案1上修改？

1. **專案1功能不多**：大部分功能未實作，歷史包袱不重
2. **已有基礎架構**：RBAC系統、資料庫結構都已建立
3. **節省時間**：不需要重新建立專案，直接擴展現有功能
4. **降低風險**：在現有基礎上擴展，比重新建立更安全

## 📝 整合策略

### 策略A：保持專案1的命名（推薦，如果已有資料）

- 保持 `permissions`, `roles`, `user_roles` 等命名
- 專案2的 `sys_admin` → 整合到現有的使用者系統或新增 `users` 表
- 專案2的 `sys_module` → 檢查是否需要新增欄位
- 專案2的 `sys_structure` → 檢查是否需要新增欄位

### 策略B：統一改為 `sys_` 前綴（如果資料不多）

- 將 `permissions` → `sys_permissions`
- 將 `roles` → `sys_roles`
- 統一命名規範，方便維護

**建議**：先檢查專案1的資料庫是否有重要資料，再決定採用哪個策略。

---

**建立日期**：2025-01-15  
**最後更新**：2025-01-15  
**更新說明**：改為直接在專案1上整合，而非建立新專案3

